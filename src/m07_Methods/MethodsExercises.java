package m07_Methods;

public class MethodsExercises {
    void main() {
        sendBienvenida();


    }


    // 1. Crea una función que imprima "¡Te doy la bienvenida al curso de Java desde cero!".
    void sendBienvenida() {
        System.out.println("¡Te doy la bienvenida al curso de Java desde cero!");
    }
    // 2. Escribe una función que reciba un nombre como parámetro y salude a esa persona.

    // 3. Haz un método que reciba dos números enteros y devuelva su resta.

    // 4. Crea un método que calcule el cuadrado de un número (n * n).

    // 5. Escribe una función que reciba un número y diga si es par o impar.

    // 6. Crea un método que reciba una edad y retorne true si es mayor de edad (y false en caso contrario).

    // 7. Implementa una función que reciba una cadena y retorne su longitud.

    //  8. Crea un método que reciba un array de enteros, calcula su media y lo retorna.

    // 9. Escribe un método que reciba un número y retorna su factorial.

    // 10. Crea una función que reciba un ArrayList<String> y lo recorra mostrando cada elemento.

    // 11. Crea un método contarOcurrencias(int[] arr, int valor) que devuelva cuántas veces aparece un valor dentro de un array.

    // 12. Crea un método buscarMayor(int[] arr) que reciba un array y devuelva el número mayor sin usar Arrays.sort().

    // 13. Crea un método filtrarPares(int[] arr) que devuelva un ArrayList<Integer> con solo los números pares del array original.

    // 14. Crea un método invertirArray(int[] arr) que devuelva un nuevo array con los elementos en orden inverso al original.

    // 15. Crea un método sumarArrayList(ArrayList<Integer> lista) que devuelva la suma de los valores de un ArrayList.

    // 16. Crea un método contarPalabras(String texto) que reciba una frase y devuelva cuántas palabras contiene (separadas por espacios).

    // 17. Crea un método eliminarDuplicados(int[] arr) que devuelva un HashSet<Integer> con los valores únicos del array.

    // 18. Crea un método frecuencias(int[] arr) que devuelva un HashMap<Integer, Integer> donde la clave sea cada número del array y el valor cuántas veces aparece.

    // 19. Crea un método mezclarArrays(int[] a, int[] b) que devuelva un nuevo array intercalando elementos de ambos arrays.
    //Si uno es más largo, añade el resto al final.

    // 20. Crea un método rotarDerecha(int[] arr, int posiciones) que mueva todos los elementos a la derecha el número indicado de posiciones, desplazando los últimos al inicio.

    // 21. Crea un método buscarSubcadena(String texto, String sub) que devuelva true si la subcadena aparece dentro del texto sin usar contains().

    // 22. Crea dos métodos con sobrecarga llamados contarCaracter:

    // - que cuente cuántas veces aparece un carácter concreto.

    // - que cuente cuántas vocales tiene el texto.

    // 23. Crea un método intercambiarPosiciones(int[] arr, int pos1, int pos2) que intercambie los valores de dos posiciones del array sin usar estructuras extra.

    // 24. Crea un método elementosComunes(int[] a, int[] b) que devuelva un HashSet<Integer> con los números que aparecen en ambos arrays.

    // 25. Crea un método analizarTexto(String frase) que devuelva un HashMap<String, Integer> donde cada palabra sea una clave y el número de veces que aparece sea el valor.
//    Ignora mayúsculas/minúsculas y signos de puntuación.

    // 26 Crea un método agruparPorLongitud(String[] palabras) que devuelva un HashMap<Integer, ArrayList<String>> donde la clave sea la longitud de palabra y el valor una lista de todas las palabras con esa longitud.

    // 27. Crea un método fusionarHashMaps(HashMap<String, Integer> m1, HashMap<String, Integer> m2) que devuelva un nuevo HashMap que combine ambos.
    //Si una clave aparece en ambos mapas, el valor debe ser la suma de ambos valores.

    // 28. Crea un método eliminarPalabrasRepetidas(ArrayList<String> lista) que elimine todas las palabras repetidas conservando solo la primera aparición, devolviendo un nuevo ArrayList.

    // 29. Crea un método indicesDe(String texto, char c) que devuelva un ArrayList<Integer> con todas las posiciones donde aparece el carácter dentro del texto.

    // 30. Crea un método topFrecuencias(int[] arr, int n) que analice el array, calcule la frecuencia de cada número y devuelva un ArrayList<Integer> con los n números que más se repiten.
    //En caso de empate, devuelve cualquiera de los candidatos con ese valor.

}

